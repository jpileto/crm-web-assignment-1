<h1>All Contacts</h1>

<ul>
  <% Contact.all.each do |contact| %>
    <li><%= contact.first_name %> <%= contact.last_name %></li>
    <li><%= contact.email %></li>
    <li><%= contact.note %></li>
    <br>
  <% end %>
</ul>



<!-- <h1>All Contacts</h1>

<ul>
<% Contact.all.each do |contact| %>
  <li><%= contact.first_name %> <%= contact.last_name %> </li>
<% end %>
</ul> -->
